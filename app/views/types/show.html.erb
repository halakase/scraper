<h1> <%= @type.name %></h1>
<ul>
  <% @type.businesses.each do |business| %>
    <li> <%= link_to business.name, type_business_path(@type, business)%></li>
  <% end %>
</ul>
<p>
  <%= link_to "Add Your Business", new_type_business_path(@type) %> |
  <%= link_to "Return to Types", types_path %>
</p>
